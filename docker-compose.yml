version: '3.9'
services:
  ghostpayments:
    build: .
    restart: unless-stopped
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
    env_file: .env
    environment:
      - DB_PATH=/app/data/ghost.db
